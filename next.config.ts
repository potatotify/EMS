import type { NextConfig } from "next";
import path from "path";

const nextConfig: NextConfig = {
  /* config options here */
  // Next.js will automatically detect src/app directory
  // No explicit configuration needed as Next.js supports src directory by default
};

export default nextConfig;
